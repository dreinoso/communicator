# ADVERTENCIA: Determinar tambien los items en el archivo contactList.py para el correcto funcionamiento del módulo.

# No borrar la siguiente linea, permite la configuración de propiedades
global priorityLevels, consoleLoggingLevel, fileLoggingLevel

# Configurar los Niveles de prioridades para los módulos de acuerdo a la siguietne escala
# 4 --> Alto 
# 3 --> Normal 
# 2 --> Bajo 
# 1 --> Muy bajo
# 0 --> Inhabilidato 
# Por defecto los niveles son
	# ethernetPriority = 4
	# bluetoothPriority = 4
	# emailPriority =  2
	# smsPriority = 0

ethernetPriority = 4
bluetoothPriority = 4
emailPriority =  2
smsPriority = 0

priorityLevels = {
'ethernet': ethernetPriority,
'bluetooth': bluetoothPriority,
'email': emailPriority,
'sms': smsPriority}

# Niveles de mensajes para notificaciones

# Especificar la habilitación de mensajes para impresiones 
	# depuración => 'DEBUG'
	# información => 'INFO'
	# advertencias => 'WARNING'
	# errores  => 'ERROR'
	# situaciones críticas => 'CRITICAL'

# Para consola por defecto se descartan los mesajes log de depuración, tomando los mensajes desde INFO
# consoleLoggingLevel = INFO
consoleLoggingLevel = 'INFO'

# Para el archivo de log por defecto se imprimen todas las notificaciones
# fileLoggingLevel = 'DEBUG'
fileLoggingLevel = 'DEBUG'